# File: dbt_project/profiles.yml
lakehouse:
  outputs:
    trino:
      type: trino
      host: trino
      port: 8080
      user: admin
      database: iceberg
      schema: gold
      threads: 4
  target: trino
