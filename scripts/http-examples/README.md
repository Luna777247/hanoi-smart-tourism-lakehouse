# HTTP debug captures

Các file `curl_*.txt` ở đây là **log verbose** (`curl -v`) khi gọi portal (OAuth2 proxy → Keycloak), không phải script chạy lại được.

Dùng để đối chiếu header redirect, cookie CSRF, hoặc troubleshooting auth local.
