# Airflow Providers (compatible with Airflow 3.x)
apache-airflow-providers-apache-spark
apache-airflow-providers-common-sql
apache-airflow-providers-fab

# OAuth2 support
authlib

# dbt & Cosmos - phiên bản >= 1.9.0 hỗ trợ Airflow 3.x
# astronomer-cosmos==1.10.6 yêu cầu Airflow >= 3.1 cho AF3 plugin
astronomer-cosmos>=1.10.6
dbt-spark[PyHive]
dbt-trino
trino

# Utils
python-dotenv

# Hanoi DAG dependencies
pydantic-settings>=2.0,<3
httpx>=0.27,<0.29
minio>=7.2,<8
