# Airflow Providers
apache-airflow-providers-apache-spark
apache-airflow-providers-common-sql
apache-airflow-providers-fab

# OAuth2 support
authlib

# dbt & Cosmos (dbt orchestration in Airflow)
astronomer-cosmos==1.11.1
dbt-spark[PyHive]
dbt-trino
trino

# Utils
python-dotenv

# Hanoi DAG hanoi_tourism_ingestion: import app.services.google_places_service (pydantic settings) + MinIO + httpx
pydantic-settings>=2.0,<3
httpx>=0.27,<0.29
minio>=7.2,<8
