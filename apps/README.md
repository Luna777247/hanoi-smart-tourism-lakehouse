# `apps/` — ứng dụng giao diện

| Thư mục    | Vai trò |
|-----------|---------|
| `backend/` | FastAPI — API portal, tích hợp Airflow / MinIO |
| `frontend/` | Next.js — UI quản trị |

Mở rộng: thêm service mới (ví dụ `apps/bff/`) trong cùng namespace này.
